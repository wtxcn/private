{
  "name": "card-offer-assistants",
  "private": true,
  "scripts": {
    "build": "node scripts/build-offers-assistants.cjs",
    "check:build": "node scripts/build-offers-assistants.cjs --check",
    "test": "node --test tests/*.test.cjs",
    "server:start": "node local-offer-server/server.cjs",
    "server:install": "./local-offer-server/install.sh"
  },
  "devDependencies": {
    "jsdom": "30.0.1"
  }
}
